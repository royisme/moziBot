# Task 02 - Build tasks menu

## Goal
Add `/tasks` command handling with `/models`-style inline button menus.

## Requirements
- Create `tasks-command.ts`
- Support list and detail rendering
- Support button-capable and text-only fallback output
- Reuse existing callback/custom_id command routing

## Validation
- Add tests for empty state, list rendering, and detail rendering
