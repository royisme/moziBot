# Local test runtime (git-ignored counterpart: .env.local)
# Copy this file to .env.local and adjust values for your machine.

# OpenAI-compatible local gateway
OPENAI_API_KEY=quotio-local-REPLACE_ME
OPENAI_BASE_URL=http://localhost:8486/v1

# Default model used in tests/config examples
MOZI_TEST_DEFAULT_MODEL=quotio/gemini-3-flash-preview
