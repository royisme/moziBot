{
  "name": "mozi-desktop-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build:web": "tsc --noEmit && vite build",
    "build": "pnpm run build:web",
    "build:native": "pnpm run build:web && pnpm tauri:build",
    "preview": "vite preview --host 127.0.0.1 --port 1421",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.5",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  }
}
